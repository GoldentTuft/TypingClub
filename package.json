{
  "devDependencies": {
    "cssnano": "^4.1.10",
    "elm-hot": "^1.1.4",
    "node-elm-compiler": "^5.0.4"
  },
  "dependencies": {
    "style.css": "^1.0.0"
  },
  "scripts": {
    "clean": "del .\\dist\\*.*",
    "dev:remort": "npm run clean && parcel index.html --public-url ./",
    "dev": "parcel index.html",
    "build": "npm run clean && parcel build index.html --public-url ./"
  }
}
